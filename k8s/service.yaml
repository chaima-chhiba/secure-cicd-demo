apiVersion: v1
kind: Service
metadata:
  name: secure-cicd-demo-service
spec:
  selector:
    app: secure-cicd-demo
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
